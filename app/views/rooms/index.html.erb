<h2>Rooms :</h2>
<%= current_user&.name %>
<br/>
<% if current_user.present? %>
  <%= link_to 'Log Out', destroy_user_session_path, class: 'btn btn-secondary', data: { turbo_method: :delete } %>
<% else %>
  <%= link_to 'Sing In', new_user_session_path, class: 'btn btn-secondary' %>
<% end %>
<%= render @rooms %>
